spring:
  application:
    name: WeTube-service
  profiles:
    default: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

geoip:
  database: classpath:GeoLite/GeoLite2-Country.mmdb