services:
  backend:
    image: ${IMAGE_NAME}:${IMAGE_TAG}
    container_name: wetube-backend-container
    ports:
      - "8080:8080"
    env_file:
      - .env.prod
    restart: always
    networks:
        - web-network

networks:
  web-network:
    external: true